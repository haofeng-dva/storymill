# 机器审核评判标准（scoring spec）

机器审核（native-check）对每章英文稿的评分规范。设计为**可扩展结构**：一代两个维度，二代追加更多维度。

## 一代评分维度（当前）

| 维度 key | 标签 | 最低门槛 | 说明 |
|---|---|---|---|
| `native` | 地道性 | ≥ 3 | 1=明显机翻/非母语，5=可发表的母语级散文 |
| `engagement` | 精彩度 | ≥ 2 | 1=平淡无聊，5=抓人的翻页感；低于 2 = 太平淡不可发布 |

**合格判定**：所有维度 ≥ 对应最低门槛，才判合格。任一维度低于门槛 → FAIL。

## 二代扩展方向（预留接口，待插入）

`SCORING_SPEC` 是一个 dict，二代直接在 `native_check.py` 的 `SCORING_SPEC` 里追加维度即可，`parse_scores` / `main` / 门槛判断会自动适配，无需改动其他代码。

计划追加的维度（候选）：

| 维度 key | 标签 | 说明 |
|---|---|---|
| `hook` | 钩子强度 | 开头/结尾钩子是否勾人（每章末的悬念） |
| `pacing` | 节奏 | 场景推进是否紧凑，有无拖沓 |
| `tension` | 张力 | 冲突/悬念的绷紧程度 |
| `character` | 人物 | 人物是否有辨识度、动机是否成立 |
| `prose` | 文笔 | 语言质感（可并入 native） |

二代插入方式示例：

```python
SCORING_SPEC = {
    "native": {...},
    "engagement": {...},
    "hook": {"label": "钩子强度", "min": 2, "desc": "1=结尾无悬念, 5=章末强钩子"},
    "pacing": {"label": "节奏", "min": 2, "desc": "1=拖沓, 5=紧凑"},
    # ... 其余维度直接追加
}
```

## 数据落盘格式

```json
{
  "file": "ch0001.md",
  "mode": "review",
  "scores": {"native": 4, "engagement": 3},
  "failed": [],
  "content": "..."
}
```

`failed` 数组列出低于门槛的维度标签，供质检层统计合格率用。
